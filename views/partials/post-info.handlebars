<article class="post">

  <div class="title">
    <a href="/post/{{id}}">
      <h3>{{title}}</h3>
    </a>
    <h4>Posted by {{user.username}}</h4>
    <a href="/post/{{id}}">{{comments.length}} comment(s)</a>
  </div>

  <div class="post-text">
    {{post_text}}
  </div>

</article>